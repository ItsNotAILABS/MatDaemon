# Repository Metadata

Suggested GitHub description:

> AI-native matrix compute platform: SDK, daemon, CLI, HTTP API, MCP server, GitHub Action, benchmarks, and optional CUDA RawKernel backend for agents, RAG, simulations, and ML automation.

Suggested homepage:

> https://github.com/ItsNotAILABS/MatDaemon#readme

Suggested topics:

- agentic-ai
- ai-agents
- artificial-intelligence
- benchmarks
- cuda
- embeddings
- fastapi
- gemm
- github-actions
- matrix-multiplication
- mcp
- mlops
- model-context-protocol
- numpy
- python
- rag
- vector-search

Suggested social cards / launch hooks:

- "Matrix compute for AI agents in one import."
- "SDK + daemon + CLI + API + MCP + GitHub Action + CUDA."
- "Expose matrix compute to AI clients without giving them shell access."
- "Run AI-shaped matrix benchmarks and publish Markdown reports."

Suggested pinned README proof points:

- `matdaemon platform` prints the shared platform contract.
- `GET /v1/platform` exposes the contract from the API.
- `matdaemon_platform_manifest` exposes the contract from MCP.
- GitHub Actions can generate benchmark JSON/Markdown artifacts.
